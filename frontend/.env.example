# =========================================================================
# WatcherAgent Frontend - Environment Configuration Example
# Copy this file to `.env` or set these variables in your hosting provider
# (e.g., Vercel, Netlify, Cloudflare Pages) during build time.
# =========================================================================

# The URL pointing to your backend API server's v1 endpoints.
# Local development fallback: http://localhost:3001/api/v1
VITE_API_BASE_URL=https://api.watcher.yourdomain.com/api/v1
